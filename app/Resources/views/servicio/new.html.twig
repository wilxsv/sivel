{% extends 'RedesSivelBundle::admin.html.twig' %}
{% block titulo %}Servicios{% endblock %}
{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" class="btn btn-success"/> <a href="{{ path('admin_servicios_index') }}" class="btn btn-default">Regresar a la lista</a>
    {{ form_end(form) }}
{% endblock %}