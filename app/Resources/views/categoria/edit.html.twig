{% extends 'RedesSivelBundle::admin.html.twig' %}
{% block titulo %}Editar Categoria{% endblock %}
{% block body %}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br/>
        <input type="submit" value="Editar" class="btn btn-info"/> <a href="{{ path('admin_categoria_index') }}" class="btn btn-default">Regresar a la lista</a> 
    {{ form_end(edit_form) }}
        <br/>
    {{ form_start(delete_form) }}<input type="submit" value="Delete" class="btn btn-danger">{{ form_end(delete_form) }}
{% endblock %}
