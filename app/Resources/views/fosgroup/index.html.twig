{% extends 'base.html.twig' %}

{% block body %}
    <h1>FosGroup list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Roles</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fosGroup in fosGroups %}
            <tr>
                <td><a href="{{ path('admin_grupo_show', { 'id': fosGroup.id }) }}">{{ fosGroup.id }}</a></td>
                <td>{{ fosGroup.name }}</td>
                <td>{% if fosGroup.roles %}{{ fosGroup.roles|join(', ') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_grupo_show', { 'id': fosGroup.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_grupo_edit', { 'id': fosGroup.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_grupo_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
