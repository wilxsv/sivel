{% extends 'RedesSivelBundle::admin.html.twig' %}
{% block titulo %}Financiamiento{% endblock %}
{% block body %}
    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ financiamiento.nombreFinanciamiento }}</td>
            </tr>
        </tbody>
    </table><br/>
    <a href="{{ path('admin_financiamiento_index') }}" class="btn btn-default">Regresar a la lista</a> <br/>
    <a href="{{ path('admin_financiamiento_edit', { 'id': financiamiento.id }) }}" class="btn btn-warning">Editar</a> {{ form_start(delete_form) }}<input type="submit" value="Borrar" class="btn btn-danger">{{ form_end(delete_form) }}
{% endblock %}